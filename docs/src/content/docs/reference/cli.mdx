---
title: CLI reference
description: Every memory subcommand with flags and examples.
sidebar:
  order: 1
---

The `memory` CLI is the reference interface to a brain. It's shipped by every SDK and exposes the same subcommand surface across TypeScript, Go, and Python.

Exit codes:

- `0` — success
- `1` — expected runtime failure (network, missing brain, invalid config)
- `2` — usage error (bad flags, missing required positional)

## Global environment variables

| Variable      | Default                      | Effect                                                     |
| ------------- | ---------------------------- | ---------------------------------------------------------- |
| `JB_HOME`     | `~/.jeffs-brain`             | Storage root used when `--brain` is omitted.               |
| `JB_BRAIN`    | unset                        | Default brain id applied when `--brain` is omitted.        |
| `JB_TOKEN`    | unset                        | Hosted bearer token. When set, flips to `HttpStore`.       |
| `JB_ENDPOINT` | `https://api.jeffsbrain.com` | Hosted API base URL.                                       |
| `OLLAMA_HOST` | `http://localhost:11434`     | Local embedder endpoint. BM25-only when unreachable.       |

## `memory init <path>`

Create a new brain directory with git-backed storage.

```bash
memory init ./my-brain
```

Output: a JSON line `{"brain":"./my-brain","ok":true}`.

## `memory ingest <file>`

Ingest a file into the brain. Chunks the content, writes it to the store, and upserts the chunks into the SQLite index.

```bash
memory ingest ./docs/hedgehogs.md --brain ./my-brain
```

Flags:

- `--brain <path>` — override the default brain directory (also set via `JB_BRAIN`).

Output: a JSON line with `path`, `hash`, `bytes`, and `skipped` (null when new, otherwise the reason).

## `memory search <query>`

Run a hybrid BM25 + vector query against the brain.

```bash
memory search "where do hedgehogs live?" --brain ./my-brain
memory search "tags:wildlife" --mode bm25 --json --limit 20
```

Flags:

- `--brain <path>` — override the default brain directory.
- `--mode <hybrid|bm25|semantic>` — default `hybrid`. Falls back to `bm25` when no embedder is configured.
- `--rerank` — apply reranking (reserved; currently a no-op).
- `--json` — emit JSON results on stdout instead of human-friendly text.
- `--limit <N>` — maximum number of results (default 10).

## `memory extract`

Submit a transcript so the server can asynchronously derive memorable facts. Delegates to the same logic as the [`memory_extract`](/spec/mcp-tools/) MCP tool.

## `memory reflect`

Close a session and trigger reflection. Mirrors [`memory_reflect`](/spec/mcp-tools/).

## `memory consolidate`

Trigger a compaction pass over `memory/` and `wiki/`. Mirrors [`memory_consolidate`](/spec/mcp-tools/). Reserved; today delegates to `brains.compile` where available.

## `memory remember` (Go only)

Capture a single, atomic memory note.

```bash
memory remember "I prefer British English in code" --tags style,english
```

## `memory recall` (Go only)

Recall memories for a query, optionally weighted by session context.

```bash
memory recall "style preferences" --limit 5
```

## `memory serve`

Start the HTTP daemon that speaks the [protocol](/spec/protocol/).

```bash
memory serve --addr 127.0.0.1:8080
```

Flags:

- `--addr <host:port>` — shared bind address flag across Go, Python, and the cross-SDK runner.
- `--root <path>` — override `JB_HOME` for this process.
- `--auth-token <token>` — optional shared bearer token for the daemon.

TypeScript also accepts the older `--host` and `--port` pair, but `--addr` is the cross-SDK canonical form.

Cross-SDK eval parity against `memory serve` is standardised around three scenarios: `ask-basic`, `ask-augmented`, and `search-retrieve-only`.
The shared runner defaults `--mode` to `auto`, matching daemon semantics and leaving resolution inside the SDK.

## `memory acl` (TS only)

Manage OpenFGA-backed authorisation when the brain is wired to `@jeffs-brain/memory-openfga`. See [`acl.ts`](https://github.com/jeffs-brain/memory/blob/main/sdks/ts/memory/src/cli/commands/acl.ts).

## `memory git` (TS only)

Helpers for the git-backed store: sign a commit, push, fetch, check status. See [`git.ts`](https://github.com/jeffs-brain/memory/blob/main/sdks/ts/memory/src/cli/commands/git.ts).

## `memory eval` (Go and TS native runners)

Run the SDK-native LongMemEval harness against the brain. The shared cross-SDK runner in [`eval/`](https://github.com/jeffs-brain/memory/tree/main/eval) only covers the daemon scenarios `ask-basic`, `ask-augmented`, and `search-retrieve-only`.

## `memory create-brain` / `memory list-brains` (Go only)

Create or list brains hosted at the configured endpoint. Mirror [`memory_create_brain`](/spec/mcp-tools/) and [`memory_list_brains`](/spec/mcp-tools/).

## Parity notes

Cross-SDK parity today is narrower than full CLI parity. The shared benchmark and docs only assume three daemon scenarios exposed by `memory serve` on every SDK: `ask-basic`, `ask-augmented`, and `search-retrieve-only`.

Full LongMemEval replay, replay ingest, and agentic loops are native-runner concerns rather than cross-SDK ones. Go provides the reference `memory eval lme run` flow; TypeScript keeps its own native `memory eval lme` commands; Python currently participates through the shared daemon scenarios.
